# staff — Multi-Agent Orchestration

Agents born with purpose. Not blank. Not configured after the fact.
TDNA insight: WEIGHTS empty at birth (experience accumulates),
but TASK populated (the agent knows its job from the start).

9 birth templates. team_task() for parallel. pipeline() for sequential.
Every agent has reports_to. K-history accumulates from failures.
